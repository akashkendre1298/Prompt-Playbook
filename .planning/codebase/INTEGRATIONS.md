# External Integrations

- **Google Fonts**: Fetched dynamically via `<link>` tag in the HTML `<head>`.
  - IBM Plex Mono
  - Syne
- **Database**: None. All data is statically coded into a `PROMPTS` array in `index.html`.
- **API**: None.
